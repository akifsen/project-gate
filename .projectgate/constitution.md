# Project Gate constitution

The auditing process must not change acceptance criteria to make a change pass.

Verifiers are read-only with respect to application source. Fixes belong to the implementation agent.

An inferred model result is not runtime evidence.

This repository's machine-checked rules live in `architecture.yml` and `rules/`.
